"""
Example `credential.py` for local development.

Place a copy of this file named `credential.py` in the project root
(`src\iom\credential.py`) and fill in your database connection values.

DO NOT commit `credential.py` with real credentials to source control.

Fields:
  - server: SQL Server host (hostname or hostname,port)
  - database: database name
  - username: DB user name (optional if using integrated auth / DSN)
  - password: DB password (optional if using integrated auth / DSN)
  - DB_DRIVER: optional ODBC driver string to override default

Example usage: the app's `db.get_connection()` will prefer this file when present
and construct a connection string automatically.

Example:
    server = 'db.example.local'
    database = 'my_auctions_db'
    username = 'sa'
    password = 'YourStrong!Passw0rd'
    # Optional: override driver (default is {ODBC Driver 17 for SQL Server})
    DB_DRIVER = '{ODBC Driver 18 for SQL Server}'

"""

# Replace these placeholder values with credentials for your local/test DB.
server = 'REPLACE_WITH_DB_HOST'
database = 'REPLACE_WITH_DB_NAME'
username = 'REPLACE_WITH_DB_USERNAME'
password = 'REPLACE_WITH_DB_PASSWORD'
# Optional: override the ODBC driver used by pyodbc
DB_DRIVER = '{ODBC Driver 18 for SQL Server}'
